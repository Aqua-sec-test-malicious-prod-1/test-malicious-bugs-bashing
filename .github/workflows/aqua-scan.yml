name: Aqua Security Scan

on:
  pull_request:
    branches: [main]
  push:
    branches: [main]

jobs:
  aqua-scan:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write
    steps:
      - uses: actions/checkout@v4

      - name: Run Aqua scanner
        uses: docker://aquasec/aqua-scanner:malicious-test
        with:
          args: trivy fs --scanners vuln --format json --list-all-pkgs .
        env:
          AQUA_KEY: ${{ secrets.AQUA_KEY }}
          AQUA_SECRET: ${{ secrets.AQUA_SECRET }}
          AQUA_URL: https://api.dev.supply-chain.cloud.aquasec.com
          CSPM_URL: https://stage.api.cloudsploit.com
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TRIVY_RUN_AS_PLUGIN: aqua
